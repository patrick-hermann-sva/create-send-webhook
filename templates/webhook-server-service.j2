[Unit]
Description={{ webhook }} webhook service
After=network.target
[Service]
Type=simple
User={{ webhook_user }}
Group={{ webhook_group }}
WorkingDirectory={{ webhook_working_dir }}
ExecStart=/usr/bin/webhook -verbose -hooks {{ webhook }} -port {{ webhook_port }}
RemainAfterExit=yes
[Install]
WantedBy=multi-user.targets
